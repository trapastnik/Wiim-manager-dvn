# План Тестирования и Развёртывания

## 📋 Текущая Ситуация

У тебя есть две ветки:
- **`main`** - стабильная версия (старый код, работает)
- **`full_refactor`** - новая версия (отрефакторенный код, не протестирован с реальными плеерами)

## 🎯 План Действий

### Этап 1: Подготовка к Тестированию (СЕЙЧАС, без плееров)

#### 1.1 Проверка синтаксиса
```bash
# На ветке full_refactor
node --check server.js

# Проверить все модули
for file in src/**/*.js; do node --check "$file"; done
for file in public/js/**/*.js; do node --check "$file"; done
```

#### 1.2 Запуск сервера в тестовом режиме
```bash
npm start
```

Проверить:
- ✅ Сервер запускается без ошибок
- ✅ Открывается http://localhost:3000
- ✅ UI загружается
- ✅ Нет ошибок в консоли браузера (F12)

#### 1.3 Что НЕ будет работать (это нормально)
- ❌ Сканирование плееров (нет устройств)
- ❌ Управление плеерами (нет устройств)
- ❌ Получение статусов (нет устройств)

Но должно работать:
- ✅ Загрузка страницы
- ✅ Переключение вкладок
- ✅ Загрузка медиафайлов
- ✅ UI элементы

---

### Этап 2: Тестирование с Плеерами (КОГДА ПОЯВИТСЯ ДОСТУП)

#### 2.1 Подготовка на ноутбуке

```bash
# 1. Убедись, что ты на ветке full_refactor
git branch
# Должно показать: * full_refactor

# 2. Если нет, переключись
git checkout full_refactor

# 3. Убедись, что у тебя последняя версия
git pull origin full_refactor

# 4. Проверь .env файл
cat .env
```

Файл `.env` должен содержать:
```
PORT=3000
WIIM_USE_HTTPS=true
WIIM_REQUEST_TIMEOUT=5000
ENABLE_STATS=false
```

#### 2.2 Запуск и тестирование

```bash
# Запустить сервер
npm start
```

**Чек-лист тестирования:**

1. **Сканирование**
   - [ ] Нажать "Сканировать сеть"
   - [ ] Плееры находятся
   - [ ] Плееры добавляются в список

2. **Добавление плеера вручную**
   - [ ] Добавить плеер по IP
   - [ ] Плеер появляется в списке

3. **Управление плеером**
   - [ ] Play/Pause работает
   - [ ] Stop работает
   - [ ] Next/Prev работает
   - [ ] Громкость меняется
   - [ ] Mute работает

4. **Статус**
   - [ ] Статус плеера обновляется
   - [ ] Отображается текущий трек
   - [ ] Отображается время воспроизведения

5. **Медиафайлы**
   - [ ] Загрузка файла работает
   - [ ] Список файлов отображается
   - [ ] Воспроизведение файла работает
   - [ ] Удаление файла работает

6. **Группы**
   - [ ] Создание группы работает
   - [ ] Управление группой работает

**Если всё работает** ✅ → переходи к Этапу 3

**Если есть ошибки** ❌ → смотри раздел "Устранение проблем" ниже

---

### Этап 3: Развёртывание на Raspberry Pi

#### 3.1 Вариант А: Слияние в main (рекомендуется)

Если всё протестировано и работает на ноутбуке:

```bash
# 1. Переключись на main
git checkout main

# 2. Слей изменения из full_refactor в main
git merge full_refactor

# 3. Запуш изменения
git push origin main

# 4. На Raspberry Pi подтяни изменения
# (подключись к Pi по SSH)
cd /path/to/wiim
git pull origin main
npm start
```

#### 3.2 Вариант Б: Тестирование full_refactor на Pi

Если хочешь сначала протестировать на Pi:

```bash
# На Raspberry Pi
cd /path/to/wiim

# Переключись на full_refactor
git checkout full_refactor

# Подтяни изменения
git pull origin full_refactor

# Запусти
npm start
```

Если работает → возвращайся к Варианту А (слей в main)

---

## 🔄 Работа с Ветками Git (для новичков)

### Основные Команды

#### Посмотреть текущую ветку
```bash
git branch
# Звёздочка (*) показывает текущую ветку
```

#### Переключиться между ветками
```bash
# Переключиться на main
git checkout main

# Переключиться на full_refactor
git checkout full_refactor
```

#### Посмотреть различия между ветками
```bash
# Что изменилось в full_refactor по сравнению с main
git diff main..full_refactor
```

#### Посмотреть список файлов, которые изменились
```bash
git diff --name-only main..full_refactor
```

#### Слияние веток
```bash
# 1. Переключись на ветку, КУДА хочешь влить изменения
git checkout main

# 2. Влей изменения ИЗ другой ветки
git merge full_refactor

# 3. Если есть конфликты, git скажет об этом
# Конфликты нужно будет разрешить вручную
```

#### Откат изменений
```bash
# Если что-то пошло не так, можно откатить последний коммит
git reset --hard HEAD~1

# Или вернуться к конкретному коммиту
git reset --hard <commit-hash>
```

---

## 🚨 Устранение Проблем

### Проблема 1: Сервер не запускается

**Ошибка**: `Cannot find module`

**Решение**:
```bash
# Проверь, что все модули на месте
ls -la src/
ls -la public/js/

# Проверь синтаксис
node --check server.js
```

**Ошибка**: `SyntaxError: Unexpected token`

**Решение**: Проблема с синтаксисом, проверь логи и найди проблемный файл

---

### Проблема 2: Плееры не находятся

**Возможные причины**:
- Неправильный IP диапазон
- Плееры в другой подсети
- Firewall блокирует сканирование

**Решение**:
```bash
# Добавь плеер вручную по IP
# В UI: "Добавить плеер вручную" → введи IP
```

---

### Проблема 3: API не работает

**Проверь**:
```bash
# Открой браузер → F12 → Console
# Посмотри на ошибки

# Проверь, что API endpoints доступны
curl http://localhost:3000/api/config/server-info
```

**Если видишь 404** → проблема с роутами
**Если видишь 500** → проблема с контроллерами

---

### Проблема 4: Конфликты при слиянии

Если при `git merge` возникли конфликты:

```bash
# Git покажет файлы с конфликтами
git status

# Открой файл с конфликтом в редакторе
# Найди строки вида:
# <<<<<<< HEAD
# код из main
# =======
# код из full_refactor
# >>>>>>> full_refactor

# Выбери нужный вариант, удали маркеры конфликта

# После разрешения всех конфликтов:
git add .
git commit -m "Разрешены конфликты слияния"
```

---

## 📊 Чек-лист Полного Цикла

### Перед тестированием (на ноутбуке)
- [ ] Переключился на `full_refactor`
- [ ] Сделал `git pull`
- [ ] Проверил `.env` файл
- [ ] Запустил `npm start`
- [ ] Открыл http://localhost:3000
- [ ] Нет ошибок в консоли

### Тестирование с плеерами (на ноутбуке)
- [ ] Сканирование работает
- [ ] Добавление плеера работает
- [ ] Play/Pause работает
- [ ] Громкость работает
- [ ] Статус обновляется
- [ ] Медиафайлы работают

### Развёртывание на Pi
- [ ] Всё работает на ноутбуке
- [ ] Слил `full_refactor` в `main`
- [ ] Запушил в GitHub
- [ ] На Pi сделал `git pull`
- [ ] На Pi запустил сервер
- [ ] Проверил основные функции

### После развёртывания
- [ ] Всё работает на Pi
- [ ] Можно удалить ветку `full_refactor` (опционально)
- [ ] Можно продолжать работу в `main`

---

## 🎓 Типичный Рабочий Процесс

### Сценарий 1: Тестирование новой версии

```bash
# На ноутбуке
git checkout full_refactor    # Переключиться на новую версию
npm start                      # Запустить
# ... тестирование ...
```

### Сценарий 2: Вернуться к старой версии

```bash
git checkout main              # Вернуться к стабильной версии
npm start                      # Запустить старую версию
```

### Сценарий 3: Утвердить новую версию

```bash
# После успешного тестирования
git checkout main              # Переключиться на main
git merge full_refactor        # Влить изменения
git push origin main           # Отправить на GitHub

# На Raspberry Pi
git pull origin main           # Скачать изменения
npm start                      # Запустить
```

### Сценарий 4: Продолжить разработку

```bash
# Если нашёл баг в full_refactor
git checkout full_refactor     # Переключиться на ветку разработки
# ... исправить баг ...
git add .
git commit -m "fix: исправлен баг X"
git push origin full_refactor
# ... повторить тестирование ...
```

---

## 💡 Полезные Советы

### 1. Всегда делай backup перед слиянием
```bash
# Создай тег с текущим состоянием main
git checkout main
git tag backup-before-refactor
git push origin backup-before-refactor
```

Если что-то пойдёт не так:
```bash
git reset --hard backup-before-refactor
```

### 2. Проверяй, на какой ветке находишься
```bash
# Перед любой командой
git branch
```

### 3. Commit часто
```bash
# Если делаешь изменения, коммить их часто
git add .
git commit -m "описание изменения"
git push
```

### 4. Используй .env для разных окружений

На ноутбуке (`.env`):
```
PORT=3000
WIIM_USE_HTTPS=true
```

На Raspberry Pi (`.env`):
```
PORT=3000
WIIM_USE_HTTPS=true
# ... может быть другие настройки для Pi
```

---

## 🆘 Экстренное Восстановление

Если всё сломалось:

### Вариант 1: Вернуться к последнему рабочему коммиту
```bash
git log --oneline           # Найди хеш последнего рабочего коммита
git reset --hard <hash>     # Вернись к нему
```

### Вариант 2: Полностью переустановить из GitHub
```bash
# ВНИМАНИЕ: это удалит все локальные изменения!
git fetch origin
git reset --hard origin/main
npm install
```

### Вариант 3: Начать заново
```bash
cd ..
mv wiim wiim-backup        # Сделать backup
git clone <repository-url>  # Склонировать заново
cd wiim
npm install
```

---

## 📞 Когда Спросить Помощь

Обращайся за помощью, если:
- ❌ Сервер не запускается и не понятно почему
- ❌ Git показывает конфликты и непонятно как разрешить
- ❌ После слияния что-то перестало работать
- ❌ Хочешь откатить изменения, но боишься что-то сломать

---

## ✅ Итоговая Рекомендация

**Пошаговый план на будущее:**

1. **Сейчас** (без плееров):
   - Оставайся на ветке `full_refactor`
   - Периодически запускай `npm start` и проверяй UI
   - Читай документацию

2. **Когда появятся плееры** (на ноутбуке):
   - Переключись на `full_refactor`
   - Протестируй все функции по чек-листу
   - Запиши все баги, если найдёшь

3. **Если всё работает**:
   - Сделай backup ветки `main`
   - Слей `full_refactor` в `main`
   - Запуши на GitHub

4. **Развёртывание на Pi**:
   - На Pi: `git pull origin main`
   - Протестируй на Pi
   - Если работает - готово! 🎉

5. **После успешного развёртывания**:
   - Можешь удалить ветку `full_refactor`: `git branch -d full_refactor`
   - Работай дальше в `main`
   - Для новых фич создавай новые ветки

---

## 📚 Дополнительные Ресурсы

### Git шпаргалка
```bash
git status              # Текущее состояние
git branch              # Список веток
git log --oneline       # История коммитов
git diff                # Что изменилось
git stash               # Временно спрятать изменения
git stash pop           # Вернуть спрятанные изменения
```

### Полезные ссылки
- Git для новичков: https://git-scm.com/book/ru/v2
- Git визуализация: https://git-school.github.io/visualizing-git/

---

**Удачи с тестированием!** 🚀

Если возникнут вопросы - спрашивай!
