# Исправление дублирования плееров при сканировании

**Дата:** 2025-12-18
**Проблема:** Дублирование плееров при сканировании сети

---

## Проблема

При использовании функции "Сканировать сеть" система добавляла уже существующие плееры повторно, если они получили новый IP-адрес от DHCP. Это приводило к дубликатам в базе данных `data/players.json`.

### Пример проблемы:

**До исправления:**
```json
{
  "players": [
    {"id": "1765472973759_i8l7g0mlu", "ip": "192.168.0.3", "name": "6 Room (window)"},
    {"id": "1766065839212_olazsgbnt", "ip": "192.168.0.3", "name": "6 Room (window)"}, // ДУБЛИКАТ!
    {"id": "1766066019307_wz00yf8jp", "ip": "192.168.0.28", "name": "3 room"},
    {"id": "1765473715735_ucgobwv1z", "ip": "192.168.0.28", "name": "3 room"}  // ДУБЛИКАТ!
  ]
}
```

**Последствия:**
- ✗ Замусоривание базы данных
- ✗ Некорректная работа AUTO-RESTORE (пытается восстановить воспроизведение на несуществующих плеерах)
- ✗ Путаница в интерфейсе (несколько плееров с одинаковыми именами)

---

## Решение

Реализована **многоуровневая система проверки дубликатов**:

### 1. Проверка по UUID (MAC-адрес) - Приоритет 1

**Самый надёжный способ** - UUID плеера не меняется даже при смене IP.

```javascript
// storage.js:89-95
if (player.uuid) {
  existingPlayer = data.players.find(p => p.uuid === player.uuid);
  if (existingPlayer) {
    console.log(`[STORAGE] Player found by UUID: ${player.uuid} → updating IP: ${existingPlayer.ip} → ${player.ip}`);
  }
}
```

### 2. Проверка по IP-адресу - Приоритет 2

Если UUID недоступен, проверяем по IP.

```javascript
// storage.js:97-103
if (!existingPlayer) {
  existingPlayer = data.players.find(p => p.ip === player.ip);
  if (existingPlayer) {
    console.log(`[STORAGE] Player found by IP: ${player.ip} → updating name: "${existingPlayer.name}" → "${player.name}"`);
  }
}
```

### 3. Проверка по имени устройства - Приоритет 3

Если IP изменился, но имя осталось прежним.

```javascript
// storage.js:105-111
if (!existingPlayer && player.name) {
  existingPlayer = data.players.find(p => p.name === player.name);
  if (existingPlayer) {
    console.log(`[STORAGE] Player "${player.name}" found by name → updating IP: ${existingPlayer.ip} → ${player.ip}`);
  }
}
```

### 4. Обновление существующего плеера

Если плеер найден, обновляем его данные **сохраняя ID**:

```javascript
// storage.js:113-118
if (existingPlayer) {
  const existingId = existingPlayer.id;
  Object.assign(existingPlayer, player);
  existingPlayer.id = existingId; // ID не меняется!
  existingPlayer.updatedAt = new Date().toISOString();
}
```

### 5. Добавление нового плеера

Только если плеер не найден ни по одному критерию:

```javascript
// storage.js:119-129
else {
  const uniqueId = `${Date.now()}_${Math.random().toString(36).substring(2, 11)}`;
  const newPlayer = {
    id: uniqueId,
    ...player,
    addedAt: new Date().toISOString()
  };
  data.players.push(newPlayer);
  console.log(`[STORAGE] New player added: "${player.name}" at ${player.ip}`);
}
```

---

## Улучшенное логирование

### В консоли сервера при сканировании:

```bash
[SCAN] ✅ Found NEW player: "6 Room (window)" at 192.168.0.3 (UUID: abc123...)
[SCAN] 🔄 Player "3 room" IP changed: 192.168.0.28 → 192.168.0.30
[SCAN] ⚪ Player "5 Room" at 192.168.0.80 - no changes
[SCAN] 🔄 Player at 192.168.0.13 name changed: "4 Room" → "4 Room (angle)"
[SCAN] Summary: 2 new, 3 updated, 3 unchanged
```

### В storage.js:

```bash
[STORAGE] Player found by UUID: abc123... → updating IP: 192.168.0.3 → 192.168.0.5
[STORAGE] Player "9 Room" found by name → updating IP: 192.168.0.41 → 192.168.0.42
[STORAGE] New player added: "2 Room" at 192.168.0.37
```

---

## API Response расширен

**Endpoint:** `POST /api/players/scan`

**Новый формат ответа:**

```json
{
  "success": true,
  "found": 8,
  "new": 2,          // Новых плееров
  "updated": 3,      // Обновлённых плееров
  "unchanged": 3,    // Без изменений
  "devices": [...],
  "subnet": "192.168.0"
}
```

**До исправления** (старый формат):
```json
{
  "success": true,
  "found": 8,
  "devices": [...],
  "subnet": "192.168.0"
}
```

---

## Дополнительные исправления

### 1. Устаревший метод `.substr()` заменён на `.substring()`

**Файл:** `storage.js:121`

**До:**
```javascript
const uniqueId = `${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
```

**После:**
```javascript
const uniqueId = `${Date.now()}_${Math.random().toString(36).substring(2, 11)}`;
```

**Причина:** `.substr()` deprecated с ES2020

### 2. Добавлено поле `updatedAt`

При обновлении существующего плеера добавляется timestamp:

```javascript
existingPlayer.updatedAt = new Date().toISOString();
```

---

## Изменённые файлы

### 1. `storage.js` (строки 78-137)
- ✅ Многоуровневая проверка дубликатов (UUID → IP → Name)
- ✅ Детальное логирование изменений
- ✅ Замена `.substr()` на `.substring()`
- ✅ Добавлено поле `updatedAt`

### 2. `server.js` (строки 627-686)
- ✅ Передача UUID из WiiM API в storage
- ✅ Подсчёт статистики (new/updated/unchanged)
- ✅ Подробное логирование сканирования
- ✅ Расширенный API response

---

## Тестовые сценарии

### ✅ Сценарий 1: Повторное сканирование без изменений

**Шаги:**
1. Запустить сканирование
2. Сразу же запустить сканирование повторно

**Ожидаемый результат:**
```
[SCAN] ⚪ Player "6 Room (window)" at 192.168.0.3 - no changes
[SCAN] ⚪ Player "5 Room" at 192.168.0.80 - no changes
...
[SCAN] Summary: 0 new, 0 updated, 8 unchanged
```

**API Response:**
```json
{"success": true, "found": 8, "new": 0, "updated": 0, "unchanged": 8}
```

---

### ✅ Сценарий 2: Плеер получил новый IP от DHCP

**Шаги:**
1. Перезагрузить плеер "9 Room"
2. DHCP выдал новый IP: 192.168.0.41 → 192.168.0.50
3. Запустить сканирование

**Ожидаемый результат:**
```
[STORAGE] Player "9 Room" found by name → updating IP: 192.168.0.41 → 192.168.0.50
[SCAN] 🔄 Player "9 Room" IP changed: 192.168.0.41 → 192.168.0.50
[SCAN] Summary: 0 new, 1 updated, 7 unchanged
```

**В `data/players.json`:**
- ✅ ID плеера НЕ изменился
- ✅ IP обновлён: 192.168.0.50
- ✅ Нет дубликатов

---

### ✅ Сценарий 3: Новый плеер в сети

**Шаги:**
1. Подключить новый WiiM плеер к сети
2. Запустить сканирование

**Ожидаемый результат:**
```
[STORAGE] New player added: "New WiiM Mini" at 192.168.0.99
[SCAN] ✅ Found NEW player: "New WiiM Mini" at 192.168.0.99 (UUID: xyz789...)
[SCAN] Summary: 1 new, 0 updated, 8 unchanged
```

---

### ✅ Сценарий 4: Переименование плеера

**Шаги:**
1. В приложении WiiM переименовать плеер: "4 Room" → "4 Room (angle)"
2. Запустить сканирование

**Ожидаемый результат:**
```
[STORAGE] Player found by IP: 192.168.0.13 → updating name: "4 Room" → "4 Room (angle)"
[SCAN] 🔄 Player at 192.168.0.13 name changed: "4 Room" → "4 Room (angle)"
[SCAN] Summary: 0 new, 1 updated, 7 unchanged
```

---

## Как проверить исправление

### 1. Запустить сервер
```bash
npm start
```

### 2. Открыть веб-интерфейс
```
http://192.168.0.X:3000
```

### 3. Перейти на вкладку "Устройства"

### 4. Нажать "🔍 Сканировать сеть"

### 5. Проверить логи в консоли сервера

**Правильный результат:**
```bash
Автоматически определена подсеть: 192.168.0.0/24
[SCAN] ⚪ Player "6 Room (window)" at 192.168.0.3 - no changes
[SCAN] ⚪ Player "5 Room" at 192.168.0.80 - no changes
...
[SCAN] Summary: 0 new, 0 updated, 8 unchanged
```

### 6. Проверить `data/players.json`

```bash
cat data/players.json | grep -c '"id"'
```

**Ожидаемый результат:** Количество ID = количество физических плееров

---

## Миграция существующей базы

Если в `data/players.json` уже есть дубликаты:

### Автоматическая очистка (рекомендуется):

1. Остановить сервер
2. Удалить файл:
   ```bash
   rm data/players.json
   ```
3. Запустить сервер
4. Запустить сканирование в UI

### Ручная очистка:

1. Остановить сервер
2. Отредактировать `data/players.json`:
   ```bash
   nano data/players.json
   ```
3. Удалить дубликаты (плееры с одинаковыми именами/IP)
4. Сохранить (Ctrl+O, Enter, Ctrl+X)
5. Запустить сервер

---

## Дополнительные улучшения (в будущем)

### 1. Endpoint для удаления недоступных плееров

```javascript
POST /api/players/cleanup
```

Проверяет доступность всех плееров и удаляет неотвечающие.

### 2. UI индикация при сканировании

Показывать в интерфейсе:
```
Сканирование завершено
✅ Найдено: 2 новых плеера
🔄 Обновлено: 3 плеера
⚪ Без изменений: 3 плеера
```

### 3. Periodic scan

Автоматическое сканирование каждые X минут (опционально).

---

## Резюме

**Проблема:** Дублирование плееров при смене IP-адреса
**Решение:** Многоуровневая проверка по UUID → IP → Name
**Файлы:** `storage.js`, `server.js`
**Статус:** ✅ Исправлено

**Теперь:**
- ✅ Нет дубликатов при повторном сканировании
- ✅ Корректное обновление IP при смене адреса
- ✅ Подробное логирование всех изменений
- ✅ Расширенная статистика в API
