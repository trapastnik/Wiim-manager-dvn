# WiiM Mini Control - Standalone версия

## ✨ Один файл со всем функционалом!

`standalone.js` - полностью автономное решение, которое:
- ✅ Работает **БЕЗ npm install**
- ✅ Не требует папки `node_modules`
- ✅ Не требует папки `public`
- ✅ Включает весь HTML/CSS/JavaScript
- ✅ Автоматически читает `.env` если есть
- ✅ Доступен с любых устройств в сети

## 🚀 Быстрый старт

### 1. Запуск на компьютере

```bash
node standalone.js
```

Вы увидите:
```
╔════════════════════════════════════════╗
║   WiiM Mini Web Control - Standalone  ║
╚════════════════════════════════════════╝

✓ Сервер запущен на порту 3000
✓ WiiM плеер: https://192.168.1.156

Доступ:
  → Локально:      http://localhost:3000
  → В сети:        http://<your-ip>:3000
```

### 2. Доступ с планшета/телефона

1. Узнайте IP адрес вашего компьютера:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```
   Например: `192.168.1.36`

2. На планшете откройте браузер и перейдите:
   ```
   http://192.168.1.36:3000
   ```

3. Готово! Управляйте WiiM с планшета

## ⚙️ Настройка

### Способ 1: Через .env файл (рекомендуется)

Создайте файл `.env` в той же папке:
```env
WIIM_IP=192.168.1.156
PORT=3000
WIIM_USE_HTTPS=true
```

### Способ 2: Редактирование standalone.js

Откройте `standalone.js` и измените строки:
```javascript
let WIIM_IP = '192.168.1.156';  // IP вашего WiiM
let PORT = 3000;                 // Порт веб-сервера
let USE_HTTPS = true;            // HTTPS для WiiM
```

## 📱 Использование на планшете

### iOS (iPad/iPhone)
1. Откройте Safari
2. Перейдите на `http://<ip-компьютера>:3000`
3. Нажмите кнопку "Поделиться" → "На экран Домой"
4. Теперь это как приложение!

### Android
1. Откройте Chrome
2. Перейдите на `http://<ip-компьютера>:3000`
3. Меню (⋮) → "Добавить на главный экран"
4. Готово!

## 🔧 Преимущества standalone версии

| Функция | Обычная версия | Standalone |
|---------|----------------|------------|
| npm install | ✅ Требуется | ❌ Не нужен |
| node_modules | ✅ Нужна папка | ❌ Не нужна |
| Папка public | ✅ Обязательна | ❌ Не нужна |
| Количество файлов | 10+ файлов | **1 файл** |
| Запуск | `npm start` | `node standalone.js` |
| Портативность | Низкая | **Высокая** |

## 💡 Сценарии использования

### Сценарий 1: Домашняя сеть
```
Компьютер (всегда включен):
  → node standalone.js

Все устройства в доме:
  → http://192.168.1.36:3000
  → Управление с телефонов, планшетов, ноутбуков
```

### Сценарий 2: Raspberry Pi
```
Raspberry Pi (работает 24/7):
  → node standalone.js
  → Доступен всегда!

Все устройства:
  → http://192.168.1.100:3000
```

### Сценарий 3: Временный доступ
```
Ноутбук:
  → node standalone.js
  → Используете несколько часов
  → Ctrl+C для остановки
```

## 🎯 Отличия от обычной версии

### Что одинаково:
- ✅ Все функции управления
- ✅ Веб-интерфейс
- ✅ Системные сообщения
- ✅ Автообновление статуса
- ✅ Адаптивный дизайн

### Что изменилось:
- 📦 Всё в одном файле
- 🚀 Быстрее запускается
- 📁 Не нужны node_modules
- 🔧 Легче переносить

## 🛠️ Устранение проблем

### Проблема: "Cannot find module 'express'"
**Решение:** Standalone НЕ использует express! Если вы видите эту ошибку, вы запускаете `server.js`. Запустите `standalone.js`

### Проблема: Плеер не отвечает
**Решение:**
1. Проверьте IP в `.env` или в коде
2. Убедитесь что WiiM включен
3. Ping: `ping 192.168.1.156`

### Проблема: Не открывается с планшета
**Решение:**
1. Проверьте что планшет в той же Wi-Fi сети
2. Убедитесь что файервол не блокирует порт 3000
3. Используйте `0.0.0.0` в коде (уже настроено)

## 📋 Системные требования

- Node.js 14+ (только для запуска сервера)
- Любой современный браузер (Chrome, Safari, Firefox)
- Wi-Fi сеть
- WiiM Mini в той же сети

## 🎉 Готово!

Теперь у вас есть полностью автономное решение в **одном файле**!

Скопируйте `standalone.js` на любой компьютер и запускайте без установки зависимостей.
