# Обновление до версии 2.0

## 🎉 Что нового:

1. ✅ **Управление несколькими плеерами** - добавляйте и переключайтесь
2. ✅ **Сканер сети** - автоматический поиск устройств
3. ✅ **Медиа библиотека** - загружайте и воспроизводите локальные файлы
4. ✅ **Сохранение плееров** - все устройства запоминаются
5. ✅ **Новый интерфейс** - 3 вкладки для удобства

## 📦 Обновление:

### Шаг 1: Установите новые зависимости

```bash
npm install
```

Добавлен пакет `multer` для загрузки файлов.

### Шаг 2: Создайте папки для данных (автоматически)

При первом запуске будут созданы:
- `data/` - хранилище плееров и медиа
- `media/` - загруженные аудио файлы

### Шаг 3: Запустите обновленный сервер

```bash
npm start
```

### Шаг 4: Откройте браузер

```
http://localhost:3000
```

## 🔄 Миграция данных:

### Автоматическая миграция:

Если у вас есть `.env` файл с настройкой `WIIM_IP`, при первом запуске:

1. Текущий плеер из `.env` будет добавлен в список
2. Он автоматически станет активным
3. Все будет работать как раньше

### Ручная настройка:

Если `.env` отсутствует или хотите добавить больше плееров:

1. Откройте вкладку "Устройства"
2. Нажмите "🔍 Сканировать сеть"
3. Или добавьте плееры вручную

## ⚠️ Важно:

### Обратная совместимость:

- ✅ Все API endpoints v1.0 работают
- ✅ Существующий `.env` файл работает
- ✅ Весь функционал управления плеером сохранен

### Новые файлы:

```
wiim/
├── data/               # НОВОЕ - хранилище данных
│   ├── players.json    # Список плееров
│   └── media.json      # Список медиа файлов
├── media/              # НОВОЕ - аудио файлы
├── storage.js          # НОВОЕ - модуль хранилища
├── server.js           # ОБНОВЛЕНО - новые API
└── public/
    ├── index.html      # ОБНОВЛЕНО - новый интерфейс
    ├── style.css       # ОБНОВЛЕНО - новые стили
    └── app.js          # ОБНОВЛЕНО - новая логика
```

### Резервные копии:

Перед обновлением созданы резервные копии:
- `server.js.backup` - старая версия сервера
- `public/app-old.js` - старая версия клиента
- `public/app.js.backup` - резерв

## 🧪 Проверка работы:

### Тест 1: Старый функционал

1. Откройте вкладку "Плеер"
2. Проверьте что управление работает
3. Проверьте громкость, play/pause

### Тест 2: Новый функционал

1. Откройте вкладку "Устройства"
2. Добавьте плеер (сканер или вручную)
3. Активируйте его
4. Вернитесь на вкладку "Плеер" - должно работать

### Тест 3: Медиа

1. Откройте вкладку "Медиа"
2. Загрузите MP3 файл
3. Нажмите "▶ Играть"
4. Проверьте воспроизведение

## 🔧 Устранение проблем:

### Проблема: "Cannot find module 'multer'"

**Решение:**
```bash
npm install multer
```

### Проблема: Старый интерфейс

**Решение:** Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### Проблема: Плеер не сохраняется

**Решение:**
1. Проверьте права на запись в папку `data/`
2. Убедитесь что файл `storage.js` существует

### Откат на v1.0:

Если нужно вернуться:

```bash
# Восстановить старый сервер
cp server.js.backup server.js

# Восстановить старый клиент
cp public/app-old.js public/app.js

# Перезапустить
npm start
```

## 📊 Проверка версии:

В консоли браузера (F12):

```javascript
// Новая версия покажет вкладки
document.querySelectorAll('.tab').length > 0
```

В консоли сервера:

```
=== WiiM Web Control ===
PORT: 3000
USE_HTTPS: true
Загружено плееров: N
```

## 🎯 Следующие шаги:

1. Просканируйте сеть и добавьте все плееры
2. Дайте им понятные имена
3. Загрузите свою музыкальную коллекцию
4. Наслаждайтесь улучшенным контролем!

---

**Вопросы?** Проверьте `FEATURES.md` для подробностей.
