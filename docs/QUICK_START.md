# 🚀 Быстрый старт WiiM Web Control

## Установка и запуск

### Что это:
- Модульная структура проекта
- Отдельные файлы HTML/CSS/JS
- Использует npm зависимости (express, multer)

### Запуск:
```bash
npm install    # Только при первом запуске
npm start
```

### Доступ:
```
http://localhost:3000
```

---

## 📱 Доступ с планшета/телефона

### 1. Запустите сервер на компьютере:
```bash
npm start
```

### 2. Узнайте IP адрес компьютера:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```
Например: `192.168.1.36`

### 3. На планшете откройте браузер:
```
http://192.168.1.36:3000
```

### 4. (Опционально) Добавьте на главный экран:
- **iOS**: Safari → Поделиться → На экран "Домой"
- **Android**: Chrome → Меню → Добавить на главный экран

---

## ⚙️ Настройка IP адреса WiiM

Файл `.env` уже создан с вашим IP:
```env
WIIM_IP=192.168.1.156
PORT=3000
WIIM_USE_HTTPS=true
```

Если IP изменился - просто отредактируйте `.env`

---

## 🎯 Итоговая инструкция:

1. Убедитесь что Node.js установлен:
   ```bash
   node --version
   ```
   Если нет - установите: https://nodejs.org

2. Установите зависимости (только первый раз):
   ```bash
   npm install
   ```

3. Запустите сервер:
   ```bash
   npm start
   ```

4. Откройте браузер:
   ```
   http://localhost:3000
   ```

5. Готово! 🎉

---

## 📚 Дополнительная документация:

- `README.md` - Полная документация
- `INSTALL.md` - Инструкции по установке Node.js
- `FEATURES.md` - Список всех функций
- `MULTI-PLAYER-GUIDE.md` - Работа с группами плееров

---

## 🆘 Помощь:

### Проблема: Node.js не установлен
```bash
# macOS
brew install node

# Или скачайте с https://nodejs.org
```

### Проблема: Плеер не отвечает
1. Проверьте IP в `.env`
2. Убедитесь что WiiM включен
3. Проверьте: `ping 192.168.1.156`

### Проблема: Не открывается с планшета
1. Убедитесь что планшет в той же Wi-Fi сети
2. Проверьте IP адрес компьютера
3. Попробуйте отключить файервол временно

---

**Всё работает!** Ваш WiiM Mini уже проверен и отвечает на запросы.
