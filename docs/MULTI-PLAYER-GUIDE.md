# Руководство по мультиплеерному управлению

## Что нового в v2.1

### Новый интерфейс первой вкладки "Плеер"

Теперь на главной вкладке отображаются **ВСЕ** плееры одновременно с индивидуальным управлением.

## Возможности:

### 1. Глобальное управление
- **"Запустить все"** - одновременно запускает воспроизведение на всех плеерах
- **"Остановить все"** - останавливает все плееры

### 2. Индивидуальные карточки плееров

Каждый плеер имеет свою карточку с:
- **Название и статус** (Играет/Пауза/Остановлен)
- **Текущий трек** (название и исполнитель)
- **Выбор медиа файла** - выпадающий список с вашими файлами
- **Кнопки управления**:
  - ▶ Играть
  - ⏸ Пауза
  - ⏹ Stop
- **Регулятор громкости** с кнопками +/-

### 3. Запоминание выбора

Система **автоматически запоминает**, какой файл вы выбрали для каждого плеера:
- Выбор сохраняется в браузере (localStorage)
- При обновлении страницы выбранные файлы остаются
- Каждый плеер может иметь свой файл

## Как использовать:

### Базовое использование:

1. **Откройте вкладку "Плеер"**
2. Увидите все добавленные плееры
3. Для каждого плеера:
   - Выберите файл из выпадающего списка
   - Нажмите "▶ Играть"
   - Используйте регулятор громкости

### Синхронное воспроизведение:

1. Выберите файлы для нужных плееров
2. Нажмите **"Запустить все"**
3. Все плееры начнут воспроизведение одновременно

### Остановка всего:

1. Нажмите **"Остановить все"**
2. Воспроизведение остановится на всех плеерах

## Визуальные индикаторы:

### Статусы плееров:
- 🟢 **Зеленая карточка** - плеер воспроизводит
- ⚪ **Белая карточка** - плеер остановлен
- 🟡 **Желтый бейдж** - плеер на паузе

### Информация в реальном времени:
- Статус обновляется каждые 3 секунды
- Отображается текущий трек
- Показывается уровень громкости

## Технические детали:

### Новые API endpoints:
- `GET /api/players/:id/status` - статус конкретного плеера
- `POST /api/players/:id/play` - воспроизведение на плеере
- `POST /api/players/:id/pause` - пауза
- `POST /api/players/:id/stop` - остановка
- `POST /api/players/:id/volume` - установка громкости

### Хранение данных:
- Выбор файлов: `localStorage.playerSelections`
- Формат: `{ playerId: filePath }`

## Примеры использования:

### Сценарий 1: Разные комнаты
```
Гостиная: Джаз
Кухня: Новости
Спальня: Классика

Выбираете файлы → Запускаете по отдельности
```

### Сценарий 2: Синхронное воспроизведение
```
Все плееры: Один и тот же трек

Выбираете один файл для всех → "Запустить все"
```

### Сценарий 3: Индивидуальная громкость
```
Каждый плеер имеет свой регулятор
Настраиваете комфортный уровень для каждой комнаты
```

## Обновление:

### Без перезапуска сервера:
1. Обновите страницу: **Ctrl+Shift+R** (Win/Linux) или **Cmd+Shift+R** (Mac)
2. Новый интерфейс загрузится автоматически

### С перезапуском (если нужно):
```bash
# Остановите сервер (Ctrl+C)
npm start
```

## Совместимость:

- ✅ Все старые функции работают
- ✅ Вкладка "Устройства" без изменений
- ✅ Вкладка "Медиа" без изменений
- ✅ Сканер сети работает как прежде

## Что осталось неизменным:

- Сканирование сети
- Добавление плееров
- Загрузка медиа файлов
- Системные сообщения
- Автообновление статуса

---

**Версия:** 2.1.0
**Дата:** 30.11.2025
**Основное изменение:** Мультиплеерное управление на главной вкладке
