# Новые функции WiiM Control Center v2.0

## 🎯 Что добавлено:

### 1. Управление несколькими плеерами ✅

**Сканер сети:**
- Автоматический поиск WiiM устройств в локальной сети
- Сканирование подсети (например, 192.168.1.0/24)
- Автоматическое добавление найденных плееров

**Ручное добавление:**
- Добавление плеера по IP адресу
- Настройка имени для каждого плеера
- Гибкая настройка HTTP/HTTPS

**Сохранение:**
- Все плееры сохраняются в `data/players.json`
- Автоматическая загрузка при следующем запуске
- Запоминание активного плеера

**Переключение:**
- Быстрое переключение между плеерами
- Активный плеер выделен в списке
- Отображение активного плеера на главной странице

### 2. Медиа библиотека ✅

**Загрузка файлов:**
- Загрузка аудио файлов через веб-интерфейс
- Поддержка форматов: MP3, FLAC, WAV, AAC, M4A, OGG
- Максимальный размер файла: 100MB

**Управление:**
- Просмотр всех загруженных файлов
- Отображение размера файлов
- Удаление ненужных файлов

**Воспроизведение:**
- Воспроизведение файлов на активном плеере одной кнопкой
- Автоматическая отправка URL файла на плеер
- Поддержка локальных файлов с сервера

### 3. Новый интерфейс с вкладками ✅

**3 вкладки:**
- **Плеер** - управление воспроизведением (сохранен весь старый функционал)
- **Устройства** - управление плеерами и сканирование
- **Медиа** - библиотека аудио файлов

**Улучшения:**
- Современный дизайн с карточками
- Адаптивная верстка для мобильных устройств
- Цветовая индикация активного плеера
- Всплывающие формы для добавления

## 📋 Как использовать:

### Первый запуск:

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Запустите сервер:**
   ```bash
   npm start
   ```

3. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

### Добавление плееров:

**Вариант 1: Автоматически (рекомендуется)**
1. Откройте вкладку "Устройства"
2. Нажмите "🔍 Сканировать сеть"
3. Введите подсеть (например, `192.168.1`)
4. Найденные плееры добавятся автоматически

**Вариант 2: Вручную**
1. Откройте вкладку "Устройства"
2. Нажмите "+ Добавить вручную"
3. Введите IP адрес (например, `192.168.1.156`)
4. Введите название (опционально)
5. Нажмите "Добавить"

### Работа с медиа файлами:

1. **Загрузка:**
   - Откройте вкладку "Медиа"
   - Нажмите "📁 Загрузить файл"
   - Выберите аудио файл
   - Дождитесь загрузки

2. **Воспроизведение:**
   - Найдите файл в списке
   - Нажмите "▶ Играть"
   - Файл начнет воспроизводиться на активном плеере

### Переключение между плеерами:

1. Откройте вкладку "Устройства"
2. Найдите нужный плеер
3. Нажмите "Активировать"
4. Вернитесь на вкладку "Плеер"
5. Теперь управление работает с выбранным плеером

## 🗂️ Структура данных:

### data/players.json
```json
{
  "players": [
    {
      "id": "1234567890",
      "ip": "192.168.1.156",
      "name": "WiiM Гостиная",
      "useHttps": true,
      "addedAt": "2025-11-30T08:00:00.000Z"
    }
  ],
  "activePlayer": "1234567890"
}
```

### data/media.json
```json
{
  "files": [
    {
      "id": "1234567891",
      "name": "song.mp3",
      "filename": "abc123",
      "path": "/media/abc123",
      "size": 5242880,
      "mimetype": "audio/mpeg",
      "addedAt": "2025-11-30T08:00:00.000Z"
    }
  ],
  "playlists": []
}
```

## 🔧 API Endpoints:

### Плееры:
- `GET /api/players` - Список всех плееров
- `POST /api/players/scan` - Сканирование сети
- `POST /api/players` - Добавить плеер вручную
- `DELETE /api/players/:id` - Удалить плеер
- `POST /api/players/:id/activate` - Установить активным

### Медиа:
- `GET /api/media` - Список файлов
- `POST /api/media/upload` - Загрузить файл
- `POST /api/media/play` - Воспроизвести файл

### Управление (без изменений):
- `GET /api/status` - Статус плеера
- `GET /api/info` - Расширенная информация
- `POST /api/control/{play|pause|stop|next|prev}` - Управление
- `POST /api/volume/{set|up|down|mute|unmute}` - Громкость

## 🎨 Что НЕ изменилось:

- ✅ Все функции управления плеером работают как раньше
- ✅ Системные сообщения отображаются
- ✅ Автообновление статуса каждые 3 секунды
- ✅ Поддержка самоподписанного SSL сертификата
- ✅ Адаптивный дизайн для планшетов

## 🚀 Совместимость:

- **Обратная совместимость:** Старые настройки из `.env` работают
- **Миграция:** При первом запуске текущий плеер из `.env` добавится автоматически
- **Без потерь:** Весь функционал v1.0 сохранен в вкладке "Плеер"

## 💡 Примеры использования:

### Сценарий 1: Несколько комнат
```
1. Добавьте все плееры через сканер
2. Назовите их: "Гостиная", "Спальня", "Кухня"
3. Переключайтесь между ними по необходимости
4. Загрузите плейлист в медиа библиотеку
5. Включайте музыку на любом плеере одной кнопкой
```

### Сценарий 2: Локальная коллекция
```
1. Загрузите свою музыкальную коллекцию
2. Создайте плейлисты (функция в разработке)
3. Воспроизводите без потоковых сервисов
4. Доступ с любого устройства в сети
```

## 🔜 Планы на будущее:

- [ ] Плейлисты
- [ ] Групповое воспроизведение (multiroom)
- [ ] Эквалайзер
- [ ] Таймер сна
- [ ] Избранное
- [ ] История воспроизведения

---

**Версия:** 2.0.0
**Дата:** 30.11.2025
